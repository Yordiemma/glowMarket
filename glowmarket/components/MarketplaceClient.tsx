"use client";
import{useMemo,useState}from"react";import{ProductCard}from"@/components/ProductCard";import type{CatalogProduct}from"@/lib/catalog";
const categories=["All","Hair Care","Skincare","Nails","Makeup","Hair Extensions","Beauty Tools"];
export function MarketplaceClient({products}:{products:CatalogProduct[]}){const[category,setCategory]=useState("All");const[query,setQuery]=useState("");const visible=useMemo(()=>products.filter(product=>(category==="All"||product.category===category)&&[product.name,product.brand,product.description,product.salon_name].join(" ").toLowerCase().includes(query.toLowerCase())),[category,query,products]);return <><label className="market-search">Search products<input type="search" value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search by product, brand, or store"/></label><div className="filters" aria-label="Product categories">{categories.map(item=><button key={item} className={category===item?"active":""} onClick={()=>setCategory(item)}>{item}</button>)}</div>{visible.length?<div className="product-grid">{visible.map(product=><ProductCard product={product} key={product.id}/>)}</div>:<div className="empty-catalog"><h2>No products found.</h2><p>Try another search or category.</p></div>}</>}
