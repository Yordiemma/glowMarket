import Link from "next/link"; import { BrandMark } from "@/components/BrandMark"; import { RegisterForm } from "@/components/RegisterForm";
export default function Register(){const demoMode=process.env.NODE_ENV==="development"&&process.env.LOCAL_AUTH_BYPASS==="true";return <main className="auth-layout"><section className="auth-art"><BrandMark/><div><p className="kicker">AI-ASSISTED BEAUTY STORES</p><h2>Upload a photo.<br/><em>AI creates the listing.</em></h2><p>A simpler way for beauty businesses to sell online without writing every product page themselves.</p></div><small>Built for Swedish beauty businesses.</small></section><section className="auth-side"><p className="auth-top">Already have an account? <Link href="/sign-in">Sign in</Link></p><RegisterForm demoMode={demoMode}/></section></main>}
